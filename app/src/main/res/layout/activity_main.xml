<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    >

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="твоя типография"
        android:textColor="#E66C02"
        android:textSize="30sp"
        app:layout_constraintLeft_toLeftOf="@+id/log1"
        app:layout_constraintRight_toRightOf="@+id/log1"
        app:layout_constraintTop_toBottomOf="@+id/log1"

        />

    <ImageView
        android:id="@+id/log1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo_1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <EditText
        app:layout_constraintTop_toBottomOf="@+id/textView"
        android:hint="логин"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="30dp"
        android:id="@+id/login"
        android:maxLength="30"
        android:maxLines="1"
        android:imeOptions="actionNext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="пароль"
        android:imeOptions="actionNext"
        android:id="@+id/password"
        android:maxLength="30"
        android:maxLines="1"
        app:layout_constraintTop_toBottomOf="@+id/login"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="16dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="войти"
        app:layout_constraintTop_toBottomOf="@id/password"
        app:layout_constraintLeft_toLeftOf="@+id/password"
        app:layout_constraintRight_toRightOf="@+id/password"
        android:layout_marginTop="20dp"
        android:background="#E66C02"/>








</androidx.constraintlayout.widget.ConstraintLayout>